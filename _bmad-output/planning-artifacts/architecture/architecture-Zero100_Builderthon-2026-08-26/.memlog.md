---
scope: Zero100_Builderthon Recruitment Handoff System
purpose: build-substrate
altitude: feature
updated: 2026-08-26T10:28
---

- (constraint) 5-day hackathon MVP scope: single-position (Backend Dev), synthetic 20 resumes, local SQLite DB, unified REST API.
- (decision) AD-1 (Criteria Versioning): Every mapping and handoff card binds immutably to a CriteriaVersionID. Official handoffs require status=APPROVED.
- (decision) AD-2 (Evidence Grounding & Coordinate Fallback): LLM extracts exact substrings with PyMuPDF character offsets & bounding boxes. Fallback to snippet+page if bbox fails.
- (decision) AD-3 (Deterministic Conflict Calculation): HR and Dev review logs are stored independently. Conflicts are computed algorithmically without AI hallucinated synthesis.
- (decision) AD-4 (Split-View Interactive Synchronization): Bidirectional event contract between PDF viewer and Criteria Panel via page and bounding-box coordinates.
- (version) Stack: Next.js 15 (App Router, React 19, Tailwind CSS, shadcn/ui) + FastAPI 0.115 (Python 3.12, Pydantic v2, PyMuPDF 1.25, SQLite/SQLAlchemy 2.0).
- (assumption) [ASSUMPTION] LLM provider: OpenAI API (gpt-4o-mini for extraction speed/cost) with structured outputs / Pydantic schema validation.
- (override) LLM provider model aligned to gpt-5.6-luna per updated PRD; PDF parser, demo volume, and pipeline assumptions remain subject to architecture synchronization.
- (event) spine finalized after removing non-essential 5-day MVP detail; no new design detail added.
- (change) Reopened the architecture for PRD synchronization: aligned the role names to HR/HM/현업 리더, bound the MVP to the B2B sales manager JD, and recorded the 178-entry ledger plus 20 resume-sample boundary.
- (override) Per user confirmation, superseded the stale Backend Dev, PyMuPDF, and gpt-4o-mini decisions with the current PRD and HR_data decisions.
- (event) spine finalized with the lean MVP architecture; manual validation used because uv and Python were unavailable.
- (decision) Epic story alignment: HandoffCard.payload_json owns question candidates, interview verifications, and final decision payloads for the MVP; separate normalized question, verification, and decision tables are deferred.
- (decision) ReviewLog keeps current judgment values and appends prior values, changes, actor, timestamp, and reason in edit_history JSON; previous mappings become INVALIDATED when Draft criteria text changes and a rerun is required.
- (decision) Evidence interaction uses Markdown text search highlighting as the default (`react-highlight-words` or `window.find()`); PDF coordinates remain optional with snippet/page/context fallback.
- (decision) Status values are two-tiered: item-level review uses the four evidence states, while document/stage-level HR and HM decisions use their separate official vocabularies; final decision accepts only 채용, 미채용, 종료, or 인재풀 등록.
- (event) Architecture spine and presentation companion synchronized to the approved epic structure; manual consistency checks completed because uv/Python are unavailable.
